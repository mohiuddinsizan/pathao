/**
 * Color palette type definitions.
 *
 * A ColorPalette maps every shadcn/ui CSS custom property
 * to an HSL value string (e.g. "205 51% 22%").
 *
 * PaletteColors keys:
 *   background, foreground, card, card-foreground,
 *   popover, popover-foreground, primary, primary-foreground,
 *   secondary, secondary-foreground, muted, muted-foreground,
 *   accent, accent-foreground, destructive, destructive-foreground,
 *   border, input, ring,
 *   chart-1..5, sidebar-background, sidebar-foreground,
 *   sidebar-primary, sidebar-primary-foreground,
 *   sidebar-accent, sidebar-accent-foreground,
 *   sidebar-border, sidebar-ring
 *
 * ColorPalette shape:
 *   { name, label, mode: "light"|"dark", swatches: [hex x4], colors: PaletteColors }
 */
