================================================================================
  MEMBER: 2105040 — ORDER DETAIL, EDIT, CANCEL & STATUS SIMULATION
================================================================================
  Branch: 2105040
  Depends on: 2105056 (DONE — project setup, auth, DB schema all ready)
================================================================================

STATUS OVERVIEW
===============
  [x] Backend: GET /api/orders/{id} — returns order + status history timeline
  [x] Backend: PATCH /api/orders/{id}/cancel — cancels pending orders
  [ ] Backend: PATCH /api/orders/{id}/status — status simulation endpoint
  [ ] Backend: PUT /api/orders/{id} — edit pending orders
  [ ] Frontend: Order detail page/panel — NOT BUILT
  [ ] Frontend: Edit order form — NOT BUILT
  [ ] Frontend: Status simulation buttons — NOT BUILT

YOUR REMAINING WORK
====================

--- BACKEND ---

  [ ] 1. Add status simulation endpoint
      File: backend/app/routers/orders.py (add to existing)

      PATCH /api/orders/{id}/status
        Body: { "status": "assigned", "note": "optional" }
        - Protected (get_current_merchant)
        - Validate transition using VALID_TRANSITIONS map
        - UPDATE orders SET status, updated_at
        - INSERT into order_status_history
        - Return updated order

      VALID_TRANSITIONS = {
          'pending':    ['assigned', 'cancelled'],
          'assigned':   ['picked_up', 'cancelled'],
          'picked_up':  ['in_transit'],
          'in_transit': ['delivered'],
          'delivered':  [],
          'cancelled':  [],
      }

  [ ] 2. Add edit order endpoint (optional)
      File: backend/app/routers/orders.py

      PUT /api/orders/{id}
        Body: { fields to update }
        - Only works when status = 'pending'
        - Returns 400 if order is not pending

--- FRONTEND ---

  [ ] 3. Order detail view (page or side panel)
      File: frontend/src/pages/OrderDetailPage.jsx (or component)
      Route: /deliveries/:orderId

      Layout:
      A. HEADER — Order ID (monospace), status badge, created date
      B. INFO CARDS — General, Recipient, Parcel info
      C. STATUS TIMELINE — vertical timeline from status_history
      D. ACTION BUTTONS — Edit (if pending), Cancel (if pending)

  [ ] 4. Status simulation panel (CRITICAL FOR DEMO)
      - Section at bottom of detail view: "Demo: Simulate Status Changes"
      - Show buttons for valid next statuses only
      - Clicking advances the order and refreshes the timeline
      - Example: pending → [Assign Driver] [Cancel]

  [ ] 5. Cancel order confirmation dialog
      - "Are you sure you want to cancel PTH-XXXXXX?"
      - Calls PATCH /api/orders/{id}/cancel (already exists)
      - Refreshes order list


WHAT'S ALREADY BUILT
======================
  ✅ GET /api/orders/{id} — returns full order + status_history array
  ✅ PATCH /api/orders/{id}/cancel — cancels pending orders
  ✅ api/orders.js getOrder(id) — fetches single order
  ✅ api/orders.js cancelOrder(id) — calls cancel endpoint
  ✅ Status history in DB + order_status_history table
  ✅ All UI components: Card, Badge, Button, Dialog


NOTES
=====
  - Status simulation is KEY for the demo — professor sees the lifecycle
  - Use Lucide icons: Clock (pending), UserCheck (assigned),
    Package (picked_up), Truck (in_transit), CheckCircle (delivered),
    XCircle (cancelled)
  - Coordinate with 2105058 on how clicking a table row opens detail
