================================================================================
  MEMBER: 2105058 — ORDER LISTING: STORE FILTER, DATE FILTER & ROW NAVIGATION
================================================================================
  Branch: 2105058
  Depends on: 2105056 (DONE — project setup, auth, DB schema all ready)
================================================================================

STATUS OVERVIEW
===============
  [x] Backend: GET /api/orders — pagination, status filter, store_id filter
  [x] Frontend: DeliveriesPage — table, status filter buttons, search, pagination
  [ ] Frontend: Store filter dropdown (Select) — API already supports it
  [ ] Frontend: Clickable rows → navigate to order detail page
  [ ] Frontend: Date range filter (optional, see note at bottom)

HOW TO RUN LOCALLY
===================
  Backend:  cd backend && uvicorn app.main:app --reload --port 8000
  Frontend: cd frontend && npm run dev
  Login:    rahim.ahmed@gmail.com / demo123
  Env file: provided by 2105056 — put it at backend/.env

IMPORTANT — HOW THE CODEBASE WORKS
=====================================
  API client: frontend/src/api/client.js
    api.get(path), api.post(path, body), etc.
  JWT auto-injected. On 401 → auto-redirects to /login.

  Order API helpers: frontend/src/api/orders.js
    getOrders({ page, limit, status, store_id })
    ← This function already handles the store_id parameter.
       It appends store_id to the query string and sends it to the backend.
       You just need to pass it.

  Store API helpers: frontend/src/api/stores.js
    getStores()  ← call this to populate the store dropdown

  DeliveriesPage.jsx currently imports: getOrders from @/api/orders,
  and several shadcn/ui components. Open the file first to understand
  the existing state variables and the shape of the orders array before
  editing.

  Key data shape of each order object:
    order.order_id   — "PTH-1" (PTH-XXXXXX string) — USE THIS for navigation
    order.id         — UUID — do NOT use this for navigation or API calls
    order.status, order.recipient_name, order.amount, order.created_at, etc.

  Available shadcn/ui: Button, Card, Badge, Input — already imported.
  Select components are NOT currently imported in DeliveriesPage.

FILES TO CHANGE  (1 file total)
=================================
  frontend/src/pages/DeliveriesPage.jsx


TASK 1 — Add store filter dropdown
=====================================
  What to add to DeliveriesPage.jsx:

  IMPORTS:
    { useNavigate } from 'react-router-dom'
    { getStores } from '@/api/stores'
    Select, SelectContent, SelectItem, SelectTrigger, SelectValue
      from '@/components/ui/select'
    (run `npx shadcn@latest add select` in the frontend/ folder if needed)

  NEW STATE:
    stores      — array, starts empty, filled by getStores() on mount
    storeFilter — string, starts as 'all'

  ON MOUNT (add to existing useEffect or create a new one):
    Call getStores(), store result in stores state.
    On error, leave stores as empty array (don't crash).

  PASS store_id TO getOrders:
    Find your existing getOrders() call. The function signature is:
      getOrders({ page, limit, status, store_id })
    Add store_id to the params you already pass:
      store_id: storeFilter !== 'all' ? storeFilter : undefined

    Also reset page to 1 when storeFilter changes (add to the dependency
    array of the existing useEffect, or add a separate useEffect).

  UI — ADD SELECT IN THE FILTER BAR:
    Place a <Select> next to your existing search input.
    value={storeFilter}
    onValueChange={v => { setStoreFilter(v); setPage(1) }}

    Contents:
      <SelectItem value="all">All Stores</SelectItem>
      {stores.map(s => (
        <SelectItem key={s.id} value={s.id}>
          {s.name}{s.branch ? ' — ' + s.branch : ''}
        </SelectItem>
      ))}

    Trigger label: "All Stores" (SelectValue placeholder)


TASK 2 — Make table rows clickable
=====================================
  What to add to DeliveriesPage.jsx:

  IMPORT:
    useNavigate from 'react-router-dom' (add to Task 1 import if not done yet)

  HOOK:
    const navigate = useNavigate()

  ON EACH <tr> IN THE TABLE BODY:
    The rows already have cursor-pointer and hover:bg-muted/50 classes.
    Add only:
      onClick={() => navigate('/deliveries/' + order.order_id)}

    Use order.order_id (the PTH-XXXXXX string), NOT order.id (UUID).
    The route /deliveries/:orderId is implemented by 2105040.

  That is the only change needed for clickable rows.


TASK 3 — Date range filter  [OPTIONAL]
=========================================
  This task requires both frontend AND backend changes. Mark it as
  optional — implement it only after Tasks 1 and 2 are done.

  BACKEND change needed first (in backend/app/routers/orders.py):
    Add date_from and date_to as optional Query params.
    When provided, add:
      AND o.created_at >= $n   (date_from)
      AND o.created_at < $n    (date_to + 1 day, to include the full day)
    to the WHERE clause.

  FRONTEND change (in DeliveriesPage.jsx):
    NEW STATE: dateFrom (string, '' default), dateTo (string, '' default)

    Pass to getOrders — but note: the existing getOrders() helper does NOT
    support date_from/date_to. You would need to either:
      a) Update getOrders() in @/api/orders.js to accept and pass them, OR
      b) Build the API URL manually in this component

    UI: Two <Input type="date"> fields plus a "Clear" button when either
    is set. Reset page to 1 when dates change.

  Note: Because this requires backend changes, coordinate with 2105040
  who is also working in orders.py.


WHAT'S ALREADY BUILT
======================
  ✅ GET /api/orders — supports: page, limit, order_status, store_id params
  ✅ DeliveriesPage.jsx — table, status filter, search, pagination all working
  ✅ getOrders() and getStores() API helpers — ready to use
  ✅ Rows already styled with cursor-pointer and hover state — just need onClick
  ✅ shadcn/ui Select is likely installable via `npx shadcn@latest add select`


NOTES
=====
  - NEVER put user input directly into SQL strings. The existing backend
    already uses parameterized queries ($1, $2, …) correctly — follow that
    pattern if you touch the backend for Task 3.
  - Any filter change (status, store, date, search) should reset page to 1.
  - Coordinate with 2105040: your row onClick navigates to
    /deliveries/${order.order_id} — their OrderDetailPage handles that route.
  - Coordinate with 2105057: after they create an order, user is redirected
    back to /deliveries — your page must still work with the current filters.
  - Do NOT make any database schema changes.
