================================================================================
  MEMBER: 2105056 — PROJECT SETUP, AUTH, SUPABASE & DEPLOYMENT
================================================================================
  Branch: 2105056
  Role:   Team lead — foundation, auth, DB, CI/CD, code review
================================================================================

STATUS: Phases 1–3 COMPLETE. Phase 4 (deployment) remaining.

PHASE 1: PROJECT SCAFFOLD  ✅ DONE
====================================

  [x] 1. Monorepo structure — frontend/ backend/ database/ plan/
  [x] 2. Backend boilerplate — FastAPI + asyncpg + pydantic-settings
  [x] 3. Database on Supabase — schema, seed, realistic data script


PHASE 2: AUTHENTICATION  ✅ DONE
===================================

  [x] 4. Auth backend — jwt_handler, password, dependencies, auth_service
  [x] 5. Auth frontend — api/client.js (fetch-based), AuthContext, Login page


PHASE 3: DESIGN SYSTEM  ✅ DONE
=================================

  [x] 6. CSS + theming — Tailwind v4 custom properties, Plus Jakarta Sans,
         red primary (0 72% 51%), dark/light toggle, matte colors
  [x] 7. AppShell layout — icon-only sidebar, resizable panels, edge pill


PHASE 4: DEPLOYMENT & CI  ⬜ REMAINING
========================================

  [ ] 8. Deploy backend to Render
      - Create Render web service pointed to backend/
      - Set env vars: DATABASE_URL, JWT_SECRET, FRONTEND_URL
      - CORS allows the Vercel frontend URL
      - Test all endpoints on deployed URL

  [ ] 9. Deploy frontend to Vercel
      - Connect repo to Vercel, root = frontend/
      - Set VITE_API_URL to the Render backend URL
      - Test deployed frontend against deployed backend

  [x] 10. CI pipeline (GitHub Actions)
      - .github/workflows/ci.yml
      - Backend: pytest 21 tests (jwt, password, schemas)
      - Frontend: vitest 7 tests (utils, api client) + build


REFERENCE FILES FOR ALL CONTRIBUTORS
======================================
  README.md             — Project overview, setup, API table
  CONTRIBUTING.md       — Branch rules, commit format, PR checklist
  database/README.md    — Database schema docs, ER diagram
  database/001_schema.sql — Source of truth for all tables
  plan/general.txt      — Architecture, status flow, team assignments

  [ ] 10. Final integration
      - Merge all feature branches into develop
      - Resolve conflicts
      - Run all tests
      - Deploy final version
      - Seed production database with demo data
      - Verify end-to-end flow on deployed app
      - Create demo script / walkthrough


DELIVERABLES CHECKLIST
======================
  [ ] Monorepo structure with frontend/ + backend/ + database/
  [ ] FastAPI running with CORS + environment config
  [ ] Supabase DB with all tables + seed data
  [ ] JWT auth working (register + login + protected routes)
  [ ] Frontend API service layer (Axios + interceptors)
  [ ] Design system CSS variables applied
  [ ] Template/guidance code for other members
  [ ] Backend deployed on Render
  [ ] Frontend deployed on Vercel
  [ ] All branches merged + tested
  [ ] Demo walkthrough script written


NOTES
=====
  - You are the bottleneck for Phase 1. Get it done ASAP so others can start.
  - Share the deployed Supabase DATABASE_URL privately (never commit it).
  - After Phase 1 is pushed, communicate to all members that they can start.
  - Keep an eye on PRs from other members — review and help resolve issues.
  - During Phase 4, coordinate with 2105039 (testing) to ensure all tests
    pass on the deployed version.
