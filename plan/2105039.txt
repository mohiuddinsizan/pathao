================================================================================
  MEMBER: 2105039 — TESTING & QUALITY ASSURANCE
================================================================================
  Branch: 2105039
  Depends on: 2105056 (DONE — project setup, auth, DB schema all ready)
  Works alongside: ALL other members — write tests as features are built
================================================================================

STATUS OVERVIEW
===============
  [x] Backend test infra: conftest.py with env vars, pytest in requirements.txt
  [x] Backend unit: test_jwt.py (7 tests) — token create, decode, tamper, empty
  [x] Backend unit: test_password.py (6 tests) — hash, verify, wrong, empty
  [x] Backend unit: test_schemas.py (8 tests) — Pydantic validation
  [x] Frontend test infra: vitest + @testing-library/react + jsdom
  [x] Frontend unit: utils.test.js (4 tests) — cn() merge, conflicts
  [x] Frontend unit: api.test.js (3 tests) — GET auth header, error, POST body
  [x] CI pipeline: .github/workflows/ci.yml — backend pytest + frontend vitest
  [ ] Backend integration tests (require live DB) — NOT BUILT
  [ ] Manual E2E test execution — NOT DONE

  Total tests passing: 28 (21 backend + 7 frontend)


YOUR REMAINING WORK
====================

  [ ] 1. Add integration tests for auth endpoints
      File: backend/tests/test_auth_integration.py
      Requires: Live DB or test DB fixture

      test_register_success — POST /api/auth/register → 200, token returned
      test_register_duplicate_email — register twice → 400/409
      test_login_success — register then login → token returned
      test_login_wrong_password — → 401
      test_get_me — login, GET /api/auth/me → merchant data
      test_get_me_no_token — → 401

      NOTE: These need an httpx AsyncClient with ASGITransport pointed at
      the FastAPI app. You need a real or mock DB connection. If using the
      real Supabase DB, use individual INSERTs (no transactions through
      pgbouncer). Clean up test data after each test.

  [ ] 2. Add integration tests for orders
      File: backend/tests/test_orders_integration.py
      Requires: Live DB

      test_create_order — POST /api/orders → 201, PTH-XXXXXX format
      test_list_orders — GET /api/orders → pagination present
      test_list_orders_filter_status — ?status=pending → correct filter
      test_list_orders_search — ?search=PTH-100001 → match
      test_get_order_detail — GET /api/orders/{id} → all fields
      test_cancel_order — PATCH /api/orders/{id}/cancel → cancelled
      test_merchant_isolation — Merchant A can't see Merchant B's orders

  [ ] 3. Add integration tests for stores
      File: backend/tests/test_stores_integration.py

      test_create_store — POST /api/stores → 201
      test_list_stores — GET /api/stores → stores returned
      test_update_store — PUT /api/stores/{id} → updated
      test_store_merchant_isolation — cross-merchant access blocked

  [ ] 4. Add more frontend component tests
      File: frontend/src/test/components/

      StatusBadge.test.js — renders correct badge color per status
      ThemeToggle.test.js — toggles dark/light class on document
      StatCard.test.js — renders icon, label, value

  [ ] 5. Run manual E2E checklist and document results
      File: backend/tests/MANUAL_E2E_RESULTS.md

      Execute the checklist below on the running app (localhost or deployed)
      and record PASS/FAIL for each item with screenshots or notes.


WHAT'S ALREADY BUILT
======================

  Files that exist NOW (do not recreate):
  ├── backend/tests/conftest.py      ← sets env vars before app imports
  ├── backend/tests/test_jwt.py      ← 7 tests (create, decode, tamper)
  ├── backend/tests/test_password.py ← 6 tests (hash, verify, wrong, empty)
  ├── backend/tests/test_schemas.py  ← 8 tests (Register, Login, Token, Merchant)
  ├── frontend/src/test/setup.js     ← imports @testing-library/jest-dom
  ├── frontend/src/test/utils.test.js← 4 tests (cn utility)
  ├── frontend/src/test/api.test.js  ← 3 tests (fetch mock, auth header)
  └── .github/workflows/ci.yml      ← runs pytest + vitest on push

  Run commands:
    Backend:  cd backend && pytest tests/ -v
    Frontend: cd frontend && npm test


MANUAL E2E TEST CHECKLIST
=========================

  Run this on the deployed or local app. Mark PASS/FAIL.

  AUTH FLOW:
    [ ] Register new account → redirected to dashboard
    [ ] Logout → redirected to login
    [ ] Login with valid credentials → see dashboard
    [ ] Login with wrong password → error message shown
    [ ] Refresh page while logged in → stays logged in (localStorage)

  DASHBOARD:
    [ ] KPI cards show correct numbers
    [ ] Recent orders table has data
    [ ] Chart renders without errors
    [ ] "Create Parcel" button visible and redirects

  DELIVERIES (ORDERS):
    [ ] Orders page shows paginated list
    [ ] Filter by status works
    [ ] Search by order ID or name works
    [ ] Pagination controls work
    [ ] Sort by column works

  STORES:
    [ ] Stores page lists all merchant stores
    [ ] Store cards show name, branch, address, phone

  DARK / LIGHT MODE:
    [ ] Toggle switches theme
    [ ] All pages readable in both modes
    [ ] Preference persists on refresh

  CROSS-CUTTING:
    [ ] Consistent red primary color scheme across pages
    [ ] Plus Jakarta Sans font everywhere
    [ ] No hardcoded/placeholder data (all from API)
    [ ] Error states handled (API down, network error)
    [ ] Sidebar navigation works for all routes


BUG REPORTING
=============

  [ ] 10. Establish bug reporting process

      When you find a bug:
        1. Note which member's code it's in
        2. Create a GitHub Issue with:
           - Title: [BUG] Brief description
           - Affected feature: Order Creation / Listing / etc.
           - Steps to reproduce
           - Expected behavior
           - Actual behavior
           - Screenshot (if UI bug)
           - Assigned to: @member_roll_number
        3. After the fix, verify and close the issue


DELIVERABLES CHECKLIST
======================
  [ ] Test infrastructure (conftest.py, fixtures, test DB)
  [ ] Auth tests (8+ test cases)
  [ ] Order creation tests (6+ test cases)
  [ ] Order listing tests (7+ test cases)
  [ ] Order detail/update/status tests (8+ test cases)
  [ ] Store tests (5+ test cases)
  [ ] Dashboard tests (3+ test cases)
  [ ] ALL tests passing on CI or local
  [ ] Manual E2E test checklist completed on deployed app
  [ ] Bug reports filed for any issues found
  [ ] Verification of bug fixes


NOTES
=====
  - Start writing tests as SOON as other members push their code.
    Don't wait for all features to be done.
  - Work closely with each member — review their code from a testing
    perspective and suggest edge cases they might have missed.
  - For the test database, the simplest approach is to:
    a. Use the same Supabase DB but wrap tests in transactions that
       rollback, OR
    b. Ask 2105056 to set up a separate test database
  - Focus on the BACKEND tests first — they're more important for the
    demo than frontend unit tests.
  - The manual E2E checklist should be run at least once on the deployed
    version before the demo.
  - Keep a count of tests written — it's impressive for the demo to say
    "we have X tests, all passing."
  - Pytest command to run all tests:
      cd backend && python -m pytest tests/ -v
